export interface IImage {
  imageUrl: string;
}
export interface IImageConfig {
  width?: string;
  maxWidth?: string;
  minWidth?: string;
  height?: string;
  maxHeight?: string;
  minHeight?: string;
}
